class Solution {
public:
    int prefixCount(vector<string>& words, string pref) {
        //int n = words.size();
        int count = 0;
        for(string& word : words){
            if(word.find(pref) == 0){
                count ++;
            }
        }
        return count;
    }
};
